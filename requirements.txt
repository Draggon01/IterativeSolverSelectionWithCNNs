# Core scientific stack
numpy>=2.0
scipy>=1.11
h5py>=3.9
scikit-learn>=1.3

# Deep learning
torch>=2.2

# Visualisation & monitoring
matplotlib>=3.8
tensorboard>=2.14

# SuiteSparse matrix downloader
ssgetpy>=1.0

# PETSc Python bindings
# petsc4py must be installed AFTER PETSc itself is on the system.
# Do NOT use this file directly for petsc4py — install it in two steps:
#
#   pip install petsc==3.25.1
#   pip install petsc4py==3.25.1 --no-build-isolation
#
# PETSc in turn requires a BLAS/LAPACK implementation:
#   sudo dnf install openblas-devel   # Fedora
#   sudo apt install libopenblas-dev  # Ubuntu/Debian
