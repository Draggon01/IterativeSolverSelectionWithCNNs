### Make benchmarking
define how much faster this is against just run all of the runners and try it out.

### Improve Predictability

- use better datasets
- use other methods to train
- use other models or somewhat like that

### Improve docs

- improve documentation on manual testing 
- improve docs on using the whole pipeline

### Misc
- test the pipeline and improve it
- make it run on my homeserver and automatically try different stuff like different data and a lot more
automatically so i can let it run and run and hopefully get improved predictions
- the automatic pipeline in the docker container should use my gpu (make sure it does)
- the automatic pipeline should extract the epochs with a excel with all the data per epoch 
- the info csv should be integrated somewhere
- create scripts that create cool images for presentation and thesis
- make thesis goal clearer
- distinctly define which parameters should be used with the cnn approach and calculate their cost for training/calculation
- vary with image size for cnn
- investigate the method of cropping images to the correct size used in the script (for this try the approach in the paper and than just count the values in a box or also summ them up so one can get a feeling if the values have a great impact or just the position of the values)
- create a script that shows a matrics system and how long every solver takes to solve it (mainly that i get a better understanding what i do)
- probably try to integrate claude code with the model and let it train it somehow if this is possible
- make the whole thing run on my windows pc in a docker container (should work but needs to be tested)
- check for preconditioner, include them somehow.



### Write Thesis
- title: Investigation of Convolutional Neural Networks for Iterative Solver Selection
- 

### make Presentation
